# Typescript Projects

This folder contains projects and exercises built with **TypeScript** to strengthen typing, error handling, and scalable code practices.

Each sub-project includes:
- TypeScript setup with `tsconfig.json`
- Features demonstrating types, interfaces, generics, and strict mode
- Practice building small functional apps in TypeScript

👉 Navigate into each folder for setup instructions.
